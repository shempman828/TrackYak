# NEW IDEAS
    add family relationships to artists in database schema


#Bug/Feature Fixes

31. When adding an artist image, I think it adds the chosen file to the db exactly where it exists. Instead, the image should be moved to the artist image directory and the path should be updated accordingly. We will need to migrate any existing paths if necessary.

32. Reduce intrusive QMessagebox calls where possible.




