# MusicLibrary runtime dependencies.
#
# Version specifiers are loose lower bounds: the floor is the version this
# project is currently developed and tested against, and newer releases are
# expected to work. Only raise a floor when a newer version is actually
# required. Transitive dependencies (certifi, idna, greenlet, ...) are left
# for pip to resolve.

# --- GUI ---
PySide6>=6.10.2

# --- Database ---
SQLAlchemy>=2.0.40

# --- Numerics / signal processing ---
numpy>=2.2.5
scipy>=1.17.1

# --- Audio I/O and playback ---
soundfile>=0.13.1
sounddevice>=0.5.5
pydub>=0.25.1

# --- Audio fingerprinting (also needs the `fpcalc` binary from chromaprint-tools) ---
pyacoustid>=1.3.1

# --- Imaging ---
pillow>=11.2.1

# --- External data sources ---
musicbrainzngs>=0.7.1
requests>=2.32.3
geopy>=2.4.1
lyriq>=1.6.0

# --- Influence graph analysis ---
networkx>=3.6.1
python-louvain>=0.16

# --- System utilities ---
psutil>=7.2.2

# --- Test suite ---
pytest>=9.1.1

# --- Optional: Linux desktop / MPRIS2 media-key integration ---
# Guarded by try/except ImportError; the app runs without them, just without
# the OS media-control bridge. Both need system libraries to build.
# dbus-python>=1.4.0
# PyGObject>=3.54.5

# --- Optional: regenerating this file ---
# pipreqs>=0.4.13
