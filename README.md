# TrackYak
A  music library and player with robust metadata and audiophile playback.

## System dependencies
Audio fingerprinting (AcoustID/chromaprint) requires the native `chromaprint` library on the host system in addition to the pip requirements — e.g. `libchromaprint1` on Debian/Ubuntu, `chromaprint` via Homebrew on macOS.
