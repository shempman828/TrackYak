NEW IDEAS
    replace many qmessageboxes with status manager
    add family relationships to artists
    ability to reorder artists?

nowplaying_view.py
    Change some chips
    chips sometimes cut off?
    If names or chips are too long, they should pan/scroll slowly
    Chips missing?
    player comes back in ctrl shft f mode after track change

player_utility.py
    windows port

player_dock.py


status_widget.py


roles_view.py


player_settings.py


menu_bar.py
    implement project support links in "about"
    mousing over the hidden bar does not show it. We should probably remove this feature.

config_setup.py
    make sure debugging level is a setting

base_album_edit.py
    changing publisher changes tab
    searching wikipedia link does not activate "wikipedia" button to appear


db_tables.py


queue_utility.py
    queue is not saving and loading properly ini does not appear to update

album_editing_relationship_helpers.py
    changing a relationship does not update tab

album_view.py
    closing album edit window changes scroll position
    two albums are being placed in the first slot at the same time

track_view.py


library_import.py
    the only date that is ever imported is year. Month and day are never imported correctly.

artist_editing


artist alias management
    allow user to define alias type with autocomplete suggestions for existing types, not a predefined dropdown.
    having action buttons in the table cell is awkward
    embed this directly into the tab instead of using a separate dialog

artist_detail_credits
    do not appear to display

wikipedia search


track_editing
    cannot move dialog independently of parent

track_edit_roles
    searching artist defaults to making new instead of matching existing even when name matches

track_edit_lyrics
    if lyrics are empty, that saves. This could be a big issue when editing multiple and the lyrics are overwritten with null

disc management
    adding a disc works but it does not appear in the track list.
    adding disc does not let you specify number?



statistics_dialog.py


genre_view
    merging fails

places_view.py
    random invalid pointer error when updating parents sometimes
    2026-03-10 23:22:43,746 - INFO - place_map.py:233 - generate_map() - Map generated with 17 valid places (filter: 8 types selected)
    INFO:musiclib:Map generated with 17 valid places (filter: 8 types selected)
    free(): invalid pointer
    AttributeError: 'ListView' object has no attribute 'view_tracks_for_selected_place'

dates_view.py


sync_view.py


award_view.py
    deleting an award does not reload the award list

influences_view.py
    disconnected "islands" of connections float away eternally

moods_view.py


publisher_view.py
    editing a publisher in any way resets the sort and scroll position
    allow multiple selection and deletion
    delete key shortcut

publisher_detail.py


artist_view.py
    dialog for suggesting close matches, ordered by closeness. For quick merging


playlist_view.py
    show track counts in parentheses (with recursive count too)

